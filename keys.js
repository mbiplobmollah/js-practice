const computer = {
    brand: 'lenevo',
    price: '45000',
    processor: 'intel',
    sdd: '512Gb',
    monitor: 'hp'
}

const keys = Object.keys(computer);
console.log(keys)
const values = Object.values(computer);
console.log(values)
